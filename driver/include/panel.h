#pragma once


/**
 * Setup the interface elements on the panel (LEDs, display, knob).
 *
 * @return true if the setup was successful
 */
bool setupPanel();

/**
 * Update the information on panel interface elements.
 */
void updatePanel();
